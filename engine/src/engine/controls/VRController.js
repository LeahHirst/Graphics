import Controller from './Controller';

export default class VRController extends Controller {
    
    constructor(camera) {
        this.camera = camera;
    }

    static checkCompatibility() {
        
    }

    update() {
        // Update camera position

    }

}